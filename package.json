{
    "name": "svelte-app",
    "version": "1.0.0",
    "scripts": {
        "deploy": "firebase use svelte-fullstack-starter && cd client && npm run build && cd ../ && firebase deploy",
        "deploy:staging": "firebase use staging && npm run build && firebase deploy",
        "start": "sirv public --single -p 5001",
        "lint": "eslint legacy/src "

    },
    "devDependencies": {
        "dotenv": "^8.2.0",
        "husky": "^4.2.5",
        "integrify": "^3.0.1",
        "prettier": "^2.0.5",
        "prettier-plugin-svelte": "^1.1.0",
        "rollup": "^2.18.1"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/QuantumInformation/svelte-netlify-identity.git"
    },
    "keywords": [
        "svelte",
        "sveltejs",
        "fullstack"
    ]
}
