{
    "rules": {
        "users": {
            ".indexOn": "username",
            "$uid": {
                ".read": "auth != null",
                ".write": "auth.uid === $uid"
            },
            "username": {
                ".validate": "newData.val() === data.val()"
            }
        }
    }
}
